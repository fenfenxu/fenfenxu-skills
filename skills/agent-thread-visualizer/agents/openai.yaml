interface:
  display_name: "Agent Thread Visualizer"
  short_description: "Agent 会话可视化 / session timeline for Cursor·Codex·Claude·Workbuddy·kimi-code — execution maps, skills/tools/sub-agents."
  default_prompt: "Use this skill’s scripts/find-thread to locate the agent thread by the session ID or name I give (current host first: Cursor, Codex, Claude Code, Workbuddy, or kimi-code). Collect stages, skills, tool calls, sub-agents (type/name), failures, and environment, then produce a layered, expandable execution map. If evidence supports it, end with brief session-health tips."
